<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <groupId>name.ousby.james</groupId>
  <artifactId>activemq-test</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>activemq-test</name>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <activemq-version>5.6.0</activemq-version>
    
    <!-- Theses deps need to match the deps used to build the activemq version 
      you are targeting -->
    <spring-version>3.0.6.RELEASE</spring-version>
    <jetty-version>7.6.1.v20120215</jetty-version>
    <camel-version>2.9.2</camel-version>
    <opensymphony-version>2.4.2</opensymphony-version>
  </properties>

  <dependencies>

    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.10</version>
      <scope>test</scope>
    </dependency>

  </dependencies>

  <build>
    <plugins>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>2.5.1</version>
        <configuration>
          <source>1.7</source>
          <target>1.7</target>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.activemq.tooling</groupId>
        <artifactId>maven-activemq-plugin</artifactId>
        <version>${activemq-version}</version>
        <configuration>
          <configUri>xbean:file:./src/test/resources/activemq/activemq.xml</configUri>
          <fork>false</fork>
          <systemProperties>
            <property>
              <name>activemq.home</name>
              <value>/usr/dev/java/servers/apache-activemq-5.6.0/</value>
            </property>
            <property>
              <name>activemq.conf</name>
              <value>./src/test/resources/activemq/</value>
            </property>
            <property>
              <name>activemq.data</name>
              <value>./target/activemq-data/</value>
            </property>
            <property>
              <name>org.apache.activemq.default.directory.prefix</name>
              <value>./target/</value>
            </property>
          </systemProperties>
        </configuration>
        <dependencies>
          <dependency>
            <groupId>org.eclipse.jetty.aggregate</groupId>
            <artifactId>jetty-all-server</artifactId>
            <version>${jetty-version}</version>
          </dependency>
          <dependency>
            <groupId>org.apache.activemq</groupId>
            <artifactId>activemq-web</artifactId>
            <version>${activemq-version}</version>
          </dependency>
          <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-web</artifactId>
            <version>${spring-version}</version>
          </dependency>
          <dependency>
            <groupId>opensymphony</groupId>
            <artifactId>sitemesh</artifactId>
            <version>${opensymphony-version}</version>
          </dependency>
          <!-- Needed by the jsp compiler to access 'javac' -->
          <!-- You will probably want to change the version number / path to suit your env. -->
          <dependency>
            <groupId>com.sun</groupId>
            <artifactId>tools</artifactId>
            <version>1.7.0_04</version>
            <scope>system</scope>
            <systemPath>${java.home}/../lib/tools.jar</systemPath>
          </dependency> 
        </dependencies>
      </plugin>

    </plugins>
  </build>
</project>
